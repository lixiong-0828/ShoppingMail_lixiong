server:
  port: 8080

spring:
  application:
    name: ShoppingMall_SpringBoot_Lixiong
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ShoppingMall_Lixiong?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: zaq1@WSXcde3
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ShoppingMallSecretKey2024LixiongVeryLongSecretKeyForJWTTokenGeneration
  expiration: 604800000

file:
  upload:
    path: ./uploads
